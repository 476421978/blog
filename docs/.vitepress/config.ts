import { defineConfig } from "vitepress"

export default defineConfig({
  // site-level options
  title: "HLG随笔",

  description: "Just playing around.",
  themeConfig: {
    // theme-level options
  },
  // ...
})
